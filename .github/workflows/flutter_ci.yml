name: Flutter CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: flutter-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: analyze-test
    runs-on: ubuntu-latest
    timeout-minutes: 22
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Maintainability guardrail tests
        run: |
          bash tool/maintainability_report_test.sh
          bash tool/feature_legacy_dependency_guard_test.sh
          bash tool/feature_presentation_error_privacy_guard_test.sh

      - name: Maintainability regression ceilings
        run: >-
          bash tool/maintainability_report.sh
          --max-boundary-refs 9
          --max-feature-presentation-db-imports 8
          --max-legacy-shim-imports 5
          --max-legacy-shims 3

      - name: Feature legacy dependency guard
        run: bash tool/feature_legacy_dependency_guard.sh

      - name: Feature presentation error privacy guard
        run: bash tool/feature_presentation_error_privacy_guard.sh

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Generate Drift code
        run: dart run build_runner build --delete-conflicting-outputs

      - name: Analyze
        run: flutter analyze

      - name: Test
        id: flutter-test
        timeout-minutes: 18
        run: |
          set -o pipefail
          mkdir -p ci-artifacts
          flutter test --timeout 2m --reporter expanded 2>&1 | tee ci-artifacts/flutter-test.log

      - name: Summarize Flutter test failure
        if: ${{ failure() && steps.flutter-test.outcome == 'failure' }}
        run: |
          {
            echo '## Flutter Test failure'
            echo
            echo 'Last 200 log lines:'
            echo
            echo '```text'
            tail -n 200 ci-artifacts/flutter-test.log
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload Flutter test diagnostics
        if: ${{ failure() && steps.flutter-test.outcome == 'failure' }}
        uses: actions/upload-artifact@v4
        with:
          name: flutter-test-diagnostics-${{ github.run_id }}-${{ github.run_attempt }}
          path: ci-artifacts/flutter-test.log
          if-no-files-found: warn
          retention-days: 7

  build-macos:
    name: build-macos-release
    if: github.event_name == 'workflow_dispatch' || (github.event_name == 'push' && github.ref == 'refs/heads/main')
    runs-on: macos-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Generate Drift code
        run: dart run build_runner build --delete-conflicting-outputs

      - name: Build macOS release app and DMG
        run: bash tool/package_macos.sh

      - name: Upload macOS release package
        uses: actions/upload-artifact@v4
        with:
          name: Bookmark-macOS
          if-no-files-found: error
          path: |
            build/macos/Build/Products/Release/Bookmark.app
            dist/macos/Bookmark-*.dmg
