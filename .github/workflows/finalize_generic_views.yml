name: Finalize generic database views

on:
  push:
    branches:
      - feature/notion-database-foundation-v2
    paths:
      - tool/finalize_generic_views.py
      - .github/workflows/finalize_generic_views.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: feature/notion-database-foundation-v2
          fetch-depth: 0
      - name: Finalize source integration
        run: python3 tool/finalize_generic_views.py
      - name: Remove one-shot patch files
        run: rm -f tool/finalize_generic_views.py .github/workflows/finalize_generic_views.yml
      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No changes"
            exit 0
          fi
          git commit -m "Use generic views for collections and synced search"
          git push origin HEAD:feature/notion-database-foundation-v2
